#include "stdafx.h"

int main(int argc, char* argv[])
{
	int n;
	printf("(1<n<=10)n=");
	scanf("%d",&n);
	int a[10],i;
    int x=0;
	for(i=0;i<n;i++){
		scanf("%d",&a[i]);
	if(a[x]<a[i])
    x=i;}
	printf("max=a[x],下标=x");
	return 0;
}

